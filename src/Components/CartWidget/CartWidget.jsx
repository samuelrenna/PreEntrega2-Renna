import React from 'react';
import './CartWidget.css';

const CartWidget = () => {
return (
<div>
    <button className="btn navbar-btn">
        <i className="bi bi-basket3 icon-lg"></i>
    </button>
    <p>99+</p>
</div>
);
};

export default CartWidget;
